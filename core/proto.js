function encodeProtobuf(data) {
  console.log('[Coffeely] Encoding protobuf (placeholder)');
  return Buffer.from([]);
}

function decodeProtobuf(buffer) {
  console.log('[Coffeely] Decoding protobuf (placeholder)');
  return {};
}

module.exports = { encodeProtobuf, decodeProtobuf };
